<%= simple_form_for([@booking, @review]) do |f| %>

  <div class="form-inputs">
    <%= f.input :rating %>
    <%= f.input :description %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>